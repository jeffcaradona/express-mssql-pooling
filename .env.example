# Application Configuration
PORT=1533
NODE_ENV=development
DEBUG=express-mssql-pooling:*

# SQL Server Configuration
# Password must be at least 8 characters with uppercase, lowercase, numbers, and special characters
MSSQL_SA_PASSWORD=YourSecurePassword123!

# Database Connection Settings
DB_HOST=localhost
DB_PORT=1433
DB_NAME=DemoApp
DB_USER=sa
DB_PASSWORD=YourSecurePassword123!

# Notes:
# - MSSQL_SA_PASSWORD and DB_PASSWORD should match
# - DB_USER is typically 'sa' for SQL Server
# - DB_HOST should be 'localhost' for local development
# - DB_PORT is 1433 by default for SQL Server
